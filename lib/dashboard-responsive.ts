/**
 * Responsive layout utilities for the WorkGraph dashboard shell.
 * @see lib/tokens/layout.ts — shellClasses
 */
export { shell as DASHBOARD_LAYOUT, shellClasses as dashClasses } from "@/lib/tokens";
