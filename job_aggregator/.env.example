# Copy to job_aggregator/.env — overrides repo-root .env.local / .env for this package.

# --- Recommended (IPv4 Session pooler on Windows; passwords may contain @) ---
# DATABASE_PASSWORD=your_database_password
# Option A — copy full host from Supabase → Connect → Session pooler (best if aws-0 vs aws-1 is unclear):
# DATABASE_POOLER_HOST=aws-1-ap-south-1.pooler.supabase.com
# Option B — region + prefix (probe-db prints both after a successful match):
# DATABASE_POOLER_AWS_PREFIX=aws-1
# DATABASE_POOLER_REGION=ap-south-1
# Discover: python -m app.main probe-db
#
# Project ref defaults from NEXT_PUBLIC_SUPABASE_URL in ../.env.local or set explicitly:
# SUPABASE_PROJECT_REF=your_project_ref

# For the pooler, prefer DATABASE_POOLER_HOST (or REGION + prefix) — do not set only
# DATABASE_HOST to *.pooler.supabase.com without DATABASE_USER=postgres.<project_ref>
# or the client may connect as user "postgres" and fail authentication.
#
# Direct Postgres host (not pooler):
# DATABASE_HOST=db.<project-ref>.supabase.co
# DATABASE_USER=postgres
# DATABASE_PORT=5432
# DATABASE_NAME=postgres

# Direct db host + IPv6 literal — only if your network has IPv6; bypass broken hostname DNS:
# DATABASE_HOST=db.<project-ref>.supabase.co
# DATABASE_HOSTADDR=<paste AAAA from nslookup>

# Aliases: SUPABASE_DB_* mirror DATABASE_*

# --- Single URI ---
# DATABASE_URL=postgresql+psycopg2://postgres:ENCODED@db.<ref>.supabase.co:5432/postgres

# --- SQLite ---
# Leave DB vars unset and omit DATABASE_URL to use ./jobs.db

# Optional overrides
# EMBEDDING_MODEL_NAME=sentence-transformers/all-MiniLM-L6-v2
# EMBEDDING_MODEL_VERSION=all-MiniLM-L6-v2-v1
# COMPANIES_JSON=./companies.json
# HTTP_TIMEOUT_SECONDS=25
# HTTP_MAX_RETRIES=4
# EMBED_BATCH_SIZE=32
