# Minimal deps for `python -m app.main ingest --no-embed` (listing sync only; no ML stack).
requests
sqlalchemy
psycopg2-binary
python-dotenv
httpx>=0.27.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0
